name: A workflow for my Hello Action
on: push

jobs:
  build:
    name: Hello Action
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
      
      - name: Run Hello Action
        uses: ./hello-action
        with:
          YOUR_NAME: "Jacob"
